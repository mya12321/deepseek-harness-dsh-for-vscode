{
  "dsh.displayName": "DeepSeek Harness(dsh) for VS Code",
  "dsh.ext.description": "Embed the local DeepSeek Harness (DSH) web UI into the VS Code auxiliary sidebar.",
  "dsh.cfg.title": "DeepSeek Harness(dsh) for VS Code",
  "dsh.cfg.port": "Port to probe / start the DSH web server.",
  "dsh.cfg.host": "Bind address. Keep 127.0.0.1 — the webview iframe only allows 127.0.0.1/localhost.",
  "dsh.cfg.autoStart": "Start the locally installed official DSH (in environment-shared share mode, the first window of the OS environment starts the one shared instance), using the selected shared or isolated DSH home and web profile. If no local package is found, an already running configured endpoint may be reused (false = reuse only).",
  "dsh.cfg.profile": "DSH profile directory name under the selected DSH home. Each VS Code window can use a different profile; the value must match ^[A-Za-z0-9._-]{1,64}$.",
  "dsh.cfg.home.mode": "DSH configuration mode. shared (default) uses the official DSH_HOME and exposes existing modules, skills, providers, credentials, and sessions. isolated uses an extension-private home that initially contains only the official profile.",
  "dsh.cfg.home.path": "Optional absolute DSH home override for shared mode. Empty follows official precedence: inherited DSH_HOME, then ~/.dsh. Machine-scoped so workspace settings cannot redirect executable DSH configuration.",
  "dsh.cfg.closePolicy": "When to stop the extension-owned DSH server. onVscodeExit (default): stop only when VS Code exits. onViewClose: also stop when the sidebar view is closed. never: never stop automatically — use the Stop DSH Server command. A reused (non-owned) instance is never stopped.",
  "dsh.cfg.share.mode": "How VS Code windows share a DSH instance. environment (default): all windows of one OS environment converge on ONE instance — Windows windows use the Windows instance, WSL windows the WSL instance; the first window starts it, the rest adopt it, and it keeps running until the last attached window exits. window: each VS Code window starts and owns its own instance (legacy). In environment mode a WSL window that has not explicitly set dsh.port uses its own default port 3081, so WSL2 localhost forwarding can never make one environment adopt the other's instance. User-managed mode (dsh.autoStart = false) keeps probing the configured port verbatim.",
  "dsh.cfg.local.packageRoot": "Optional absolute path to the official @deepseek-ai/dsh package root. Empty = detect the global npm installation automatically.",
  "dsh.cfg.local.nodePath": "Optional absolute path to the Node.js executable used to launch the official DSH package. Empty = detect Node.js automatically.",
  "dsh.cfg.executablePath": "Optional absolute path to the dsh executable: the package directory, its lib/bin.js entrypoint, or a Windows shim (dsh.cmd / dsh.ps1). Empty = detect automatically. Takes precedence over automatic discovery.",
  "dsh.cfg.launch.method": "How DSH is launched. auto (default): resolve the installed official package, then fall back to the dsh command from PATH. managed: only the installed package. command: only the dsh command from PATH (see dsh.launch.command).",
  "dsh.cfg.launch.command": "The dsh command used when dsh.launch.method is command (or the auto fallback). A name resolved from PATH, default dsh.",
  "dsh.cfg.extraArgs": "Extra CLI arguments appended to every extension-owned dsh web launch (e.g. [\"--patch\", \"C:\\overlay.yml\"]). The extension-owned --host/--port/--profile flags always come first.",
  "dsh.cfg.runtime.manifestUrl": "Optional HTTPS runtime release manifest (schemaVersion 1, artifacts with embedded manifests). Empty = use the locally installed official npm DSH; non-empty = opt into verified managed-runtime provisioning.",
  "dsh.cfg.runtime.version": "Optional managed-runtime version pin; used only when dsh.runtime.manifestUrl is configured.",
  "dsh.cfg.features.clipboard-bridge": "Embedded copy/paste bridge between the DSH iframe and the VS Code clipboard.",
  "dsh.cfg.features.thread-attachment": "Add the active file/selection/problems to the DSH conversation (Add to Thread commands and thread module).",
  "dsh.cfg.features.editor-links": "Open DSH Read… and draft attachment links in this VS Code window (text document bridge).",
  "dsh.cfg.features.statusbar-basic": "Basic DSH status indicator in the status bar.",
  "dsh.cfg.features.theme-follow": "Follow the VS Code active color theme in the embedded DSH iframe (dark/light).",
  "dsh.cfg.keybindings.ctrlL": "Enable the Ctrl+L (Cmd+L on macOS) keybinding that adds the active selection to the DSH conversation as a draft (nothing is sent automatically). On by default.",
  "dsh.cmd.newInstance": "New DSH Instance",
  "dsh.cfg.multiInstance.entry": "Show the new-instance entry in the DSH sidebar title bar. On by default.",
  "dsh.view.welcome.changes": "DSH edits pushed through the bridge will appear here for review (diff / accept / undo).",
  "dsh.cfg.bridge.terminal": "Let DSH use VS Code terminals through the bridge (create/send/read, max 8). Off by default.",
  "dsh.cfg.bridge.editorRead": "Let DSH read the active editor's unsaved buffer through the bridge (editor/read). Off by default.",
  "dsh.cfg.bridge.ui": "Let DSH show user-visible VS Code surfaces through the bridge (window messages, progress notifications, status bar, output channel, confirm/ask prompts). Off by default.",
  "dsh.view.title": "DSH",
  "dsh.view.name": "DSH",
  "dsh.view.changes": "DSH Changes",
  "dsh.cap.untrusted": "Launches a local process and touches workspace files.",
  "dsh.cap.virtual": "Requires the dsh CLI and a real file system.",
  "dsh.cmd.category": "DeepSeek Harness",
  "dsh.cmd.openInBrowser": "Open DSH in Browser",
  "dsh.cmd.restartServer": "Restart DSH Server",
  "dsh.cmd.stopServer": "Stop DSH Server",
  "dsh.cmd.focusSidebar": "Focus DSH Sidebar",
  "dsh.cmd.addActiveFile": "Add Active File to DSH Context",
  "dsh.cmd.addActiveSelection": "Add Active Selection to DSH Context",
  "dsh.cmd.addSelectionToThread": "Add to DSH Thread",
  "dsh.cmd.addFileToThread": "Add File to DSH Thread",
  "dsh.cmd.addFolderToThread": "Add Folder to DSH Thread",
  "dsh.cmd.addProblems": "Add Problems to DSH Context",
  "dsh.cmd.newSession": "New Session",
  "dsh.cmd.switchSession": "Switch Session",
  "dsh.cmd.openSessionHistory": "Open Session History",
  "dsh.cmd.capabilities": "Capabilities and Integrations",
  "dsh.cmd.diagnose": "Diagnose",
  "dsh.cmd.cleanupOrphans": "Clean Up Orphan DSH Servers",
  "dsh.cmd.restartClean": "Restart DSH Server Cleanly",
  "dsh.cmd.onboarding": "Set up DSH",
  "dsh.cmd.changes.openDiff": "Open Diff",
  "dsh.cmd.changes.accept": "Accept Change",
  "dsh.cmd.changes.undo": "Undo Change",
  "dsh.cmd.changes.refresh": "Refresh Changes",
  "dsh.cmd.changes.focus": "Focus DSH Changes",
  "dsh.cmd.changes.toggleScope": "Toggle Changes View: This Session / All",
  "dsh.cmd.ctrlKEdit": "Edit with DSH (Ctrl+K)",
  "dsh.cmd.ctrlIEdit": "Edit with DSH Files (Ctrl+I)",
  "dsh.cmd.mcp.refresh": "Refresh MCP Servers",
  "dsh.cmd.fim.setApiKey": "Set DSH FIM API Key",
  "dsh.cmd.mcp.forgetConsent": "Forget MCP Server Consent",
  "dsh.lm.vendor": "DSH",
  "dsh.participant.name": "DSH",
  "dsh.participant.description": "Ask the local DeepSeek Harness (DSH) from the VS Code chat view. Off by default; enable with dsh.features.chat-participant.",
  "dsh.cfg.features.changes-review": "Review workspace edits proposed by DSH: approval prompts, the dsh.changes tree view, and the vscode/changes/push bridge handler. On by default (recommended); every edit still needs explicit approval.",
  "dsh.cfg.changes.observe-tools": "Record DSH edit/write tool writes in the changes tree (observation and attribution only - execution and permissions are governed by the DSH sandbox).",
  "dsh.cfg.features.ctrl-k": "Enable the Edit with DSH command: select code, press Ctrl+K (Cmd+K), type an instruction. Off by default; while disabled the Ctrl+K keybinding stays inactive.",
  "dsh.cfg.features.ctrl-i": "Enable the Edit with DSH Files (Ctrl+I) command that lets you pick 1-8 workspace files and sends them as a multi-file context block to the DSH conversation. Off by default; no keybinding is contributed.",
  "dsh.cfg.features.lm-route": "Expose DSH models to the VS Code language-model chat picker (vendor dsh). Off by default.",
  "dsh.cfg.lm.route": "DSH model routing mode. off = never register the dsh chat provider; fixed = fetch /api/lm/models once and cache; dynamic = refresh the model list on every picker open.",
  "dsh.cfg.features.mcp-consume": "Let DSH consume VS Code MCP servers through the bridge (vscode/mcp/listServers|listTools|callTool). Off by default.",
  "dsh.cfg.features.call-export": "Let DSH call exported methods of installed VS Code extensions through the bridge (vscode/extensions/callExport). Off by default; activation and invocation share this consent gate.",
  "dsh.cfg.features.exports": "Enable the programmatic exports API returned by the extension activate() face (ask/listSessions/addContext). Off by default; calls throw DSH_EXPORT_DISABLED until enabled.",
  "dsh.cfg.features.chat-participant": "Enable the @dsh chat participant in the VS Code chat view. On by default (recommended); the participant consumes DSH sessions only and never uses the vscode.lm/Copilot quota.",
  "dsh.cfg.features.tab-completion": "Enable the DSH FIM tab-completion provider for file documents. Off by default; POC-quality feature.",
  "dsh.cfg.fim.baseUrl": "Upstream FIM endpoint (full URL of an OpenAI-compatible completions API, e.g. https://api.deepseek.com/beta/completions). Set together with the DSH FIM API Key; changes take effect after restarting the DSH server.",
"dsh.cfg.fim.model": "DSH-side FIM model name used for tab completion. The upstream API key and base URL are configured in the DSH-side vscode-fim plugin, never here."
}
